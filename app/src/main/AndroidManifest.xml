<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aluno.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResultAnuncioActivity" android:label="Detalhes" android:parentActivityName=".ListaAnuncioActivity"></activity>
        <activity
            android:name=".ListaAnuncioActivity"
            android:label="OLX FAKE">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaComidasActivity"></activity>
        <activity android:name=".ItemCelular" />
        <activity android:name=".ListaCelularActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>